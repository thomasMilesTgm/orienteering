[package]
name = "orienteering"
version = "0.1.0"
edition = "2024"

[features]
default = ["serde"]
serde = ["dep:serde"]

[dependencies]
nalgebra = "0.33.2"
rand = "0.9.1"
serde = { version = "1.0.219", features = ["derive"], optional = true }
